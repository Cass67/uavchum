---
# The `tar` task in deploy/deploy.yml creates a deployment archive; ansible has
# no core module for archive creation (unarchive is extraction-only), so
# command-instead-of-module is a false positive there. Downgraded to a warning
# so real shell-vs-module violations still fail the build.
warn_list:
  - command-instead-of-module
